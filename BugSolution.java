public class MyClass {
    public static void main(String[] args) {
        int[] arr = new int[5];
        if (0 <= 2 && 2 < arr.length) { // Check the array index
            System.out.println(arr[2]); // Access a valid element
        }
    }
}